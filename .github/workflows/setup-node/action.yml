name: setup-node
description: 'Setup Node.js ⚙️ - Cache dependencies ⚡ - Install dependencies 🔧'
runs:
  using: 'composite'
  steps:
    - name: Setup Node.js ⚙️
      uses: actions/setup-node@v4
      with:
        node-version: 20
        registry-url: 'https://registry.npmjs.org'
        cache: 'npm' # Это автоматически кэширует npm зависимости

    - name: Install dependencies 🔧
      shell: bash
      run: npm ci # Гарантированно ставит точные версии зависимостей
